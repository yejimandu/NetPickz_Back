apiVersion: v1
kind: Service
metadata:
  namespace: netpickz-test
  name: netpickz-back
  labels:
    part-of: k8s-netpickz
    component: backend-server
    name: netpickz-back
    instance: netpickz-back
    version: 1.0.1
    managed-by: kubectl
spec:
  selector:
    part-of: k8s-netpickz
    component: backend-server
    name: netpickz-back
    instance: netpickz-back
  ports:
    - port: 80
      targetPort: 8081
      nodePort: 32122
  type: NodePort
