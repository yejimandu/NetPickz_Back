spring:
  application:
     name : Netpickz_back
  # database 연결
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mariadb://localhost:3306/netpickz
    username: root
    password: 181128
    driver-class-name: org.mariadb.jdbc.Driver 
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none 
#      ddl-auto: create 
  mail:
    host: smtp.gmail.com
    port: 587
    username: jeonsongyong27@gmail.com
    password: qozi ilrs rbgk qksz
    properties:
     mail: 
      smtp:
       auth: true	
       starttls:
        enable: true    
  data:
    redis:
      host: localhost  # Redis 서버 주소
      port: 6379       # 기본 포트   
server:
  servlet:
    context-path: /
    
logging:
  level:
    reactor.netty.http.client: DEBUG

#springdoc:
#  swagger-ui:
#    path :  /swagger-ui.html
  #api-docs:
   # path: /home
   
  